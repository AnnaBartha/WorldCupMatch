server:
  port: 8082

spring:
  application:
    name: match-service

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3305/match_db}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:BarthaAlma2002}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health